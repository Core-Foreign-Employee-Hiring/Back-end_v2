name: rhkr8521 CI/CD

on:
  push:
    branches: [ "develop" ]

permissions:
  contents: read

jobs:
  # Spring Boot 애플리케이션을 빌드하여 도커허브에 푸시하는 과정
  build-docker-image:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    # 1. Java 17 세팅
    - name: Set up JDK 17
      uses: actions/setup-java@v3
      with:
        java-version: '17'
        distribution: 'temurin'

    # 2-1. application.yml 파일 생성
    - name: Create application.yml
      run: |
        mkdir -p src/main/resources
        echo "${{ secrets.APPLICATION_YML }}" > src/main/resources/application.yml

    # 2-2. application-jwt.yml 파일 생성
    - name: Create application-jwt.yml
      run: |
        mkdir -p src/main/resources
        echo "${{ secrets.APPLICATION_JWT_YML }}" > src/main/resources/application-jwt.yml

    # 2-3. application-mail.yml 파일 생성
    - name: Create application-mail.yml
      run: |
        mkdir -p src/main/resources
        echo "${{ secrets.APPLICATION_MAIL_YML }}" > src/main/resources/application-mail.yml

    # 2-4. application-sms.yml 파일 생성
    - name: Create application-sms.yml
      run: |
        mkdir -p src/main/resources
        echo "${{ secrets.APPLICATION_SMS_YML }}" > src/main/resources/application-sms.yml

    # 2-5. application-business.yml 파일 생성
    - name: Create application-business.yml
      run: |
        mkdir -p src/main/resources
        echo "${{ secrets.APPLICATION_BUSINESS_YML }}" > src/main/resources/application-business.yml

    # 2-6. application-aws.yml 파일 생성
    - name: Create application-aws.yml
      run: |
        mkdir -p src/main/resources
        echo "${{ secrets.APPLICATION_AWS_YML }}" > src/main/resources/application-aws.yml

    # 2-7. application-toss.yml 파일 생성
    - name: Create application-toss.yml
      run: |
        mkdir -p src/main/resources
        echo "${{ secrets.APPLICATION_TOSS_YML }}" > src/main/resources/application-toss.yml

    # 2-8. application-spec.yml 파일 생성
    - name: Create application-spec.yml
      run: |
        mkdir -p src/main/resources
        echo "${{ secrets.APPLICATION_SPEC_YML }}" > src/main/resources/application-spec.yml

    # 3. Gradle 설정
    - name: Setup Gradle
      uses: gradle/actions/setup-gradle@v4

    # 4. Spring Boot 애플리케이션 빌드
    - name: Grant execute permission for gradlew
      run: chmod +x ./gradlew
    
    - name: Build with Gradle
      run: ./gradlew clean bootJar

    # 5. Docker 이미지 빌드
    - name: docker image build
      run: docker build -t ${{ secrets.DOCKERHUB_USERNAME }}/foreign_cicd_action .

    # 6. DockerHub 로그인
    - name: docker login
      uses: docker/login-action@v3
      with:
        username: ${{ secrets.DOCKERHUB_USERNAME }}
        password: ${{ secrets.DOCKERHUB_PASSWORD }}

    # 7. Docker Hub 이미지 푸시
    - name: docker Hub push
      run: docker push ${{ secrets.DOCKERHUB_USERNAME }}/foreign_cicd_action

# 위 과정에서 푸시한 이미지를 ec2에서 풀받아서 실행시키는 과정 
  run-docker-image-on-ec2:
    # build-docker-image (위)과정이 완료되어야 실행됩니다.
    needs: build-docker-image
    runs-on: self-hosted

    steps:
      # 1. 최신 이미지를 풀받습니다
      - name: docker pull
        run: sudo docker pull ${{ secrets.DOCKERHUB_USERNAME }}/foreign_cicd_action
      
      # 2. 기존의 컨테이너를 중지시킵니다
      - name: docker stop container
        run: sudo docker rm -f foreign 2>/dev/null || true

      # 3. 최신 이미지를 컨테이너화하여 실행시킵니다
      - name: docker run new container
        run: sudo docker run --name foreign -d -p 8080:8080 -m 700m ${{ secrets.DOCKERHUB_USERNAME }}/foreign_cicd_action

      # 4. 미사용 이미지를 정리합니다
      - name: delete old docker image
        run: sudo docker system prune -f
